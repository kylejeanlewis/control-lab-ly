setup:
  module: Compound
  class: LiquidMover.LiquidMoverSetup
  settings:
    component_config:
      mover: 
        module: Move
        class: Cartesian.Primitiv
        settings:
          port: COM17
          limits: [[-470,0,0], [0,0,0]]